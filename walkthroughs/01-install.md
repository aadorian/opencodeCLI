## Install OpenCode

OpenCode is a CLI tool that helps you with software engineering tasks. Install it globally via npm:

```bash
npm install -g opencode
```

> 💡 **Tip:** If you prefer not to install globally, you can also use `npx opencode` to run it without installation.

### Verify Installation

Check that OpenCode was installed correctly:

```bash
opencode --version
```
